# Admin auth
ADMIN_USER=admin
ADMIN_PASSWORD=change-me

# Flask
FLASK_SECRET_KEY=replace-with-32chars-min

# Encrypt OTP secrets (Fernet)
# Generate with:
# python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"
FERNET_KEY=PASTE_FERNET_KEY_HERE

# DB
DATABASE_URL=sqlite:///otp.db

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=false
